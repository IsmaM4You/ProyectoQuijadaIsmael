import 'package:flutter/material.dart';

class RespuestasPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Respuestas'),
      ),
      body: Center(
        child: Text('Contenido de Respuestas'),
      ),
    );
  }
}
